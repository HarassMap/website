columns:
    id:
        label: 'harassmap.incidents::lang.form.id'
    public_id:
        label: 'harassmap.incidents::lang.form.public_id'
    date:
        label: 'harassmap.incidents::lang.form.date_report'
        type: datetime
    created_at:
        label: 'harassmap.incidents::lang.form.date_reported'
        type: datetime
    verified:
        label: 'harassmap.incidents::lang.form.verified'
        type: switch
    is_intervention:
        label: 'harassmap.incidents::lang.form.intervention'
        type: switch
    user:
        label: 'harassmap.incidents::lang.form.user'
        relation: user
        select: 'concat(name, '' '', surname)'
    support:
        label: 'harassmap.incidents::lang.form.support'
    description:
        label: Description
        searchable: true
        invisible: true