columns:
    description:
        label: 'harassmap.incidents::lang.form.description'
        type: text
    date:
        label: 'harassmap.incidents::lang.form.date'
        type: text
    created_at:
        label: 'harassmap.incidents::lang.form.date_reported'
        type: text
    verified:
        label: 'harassmap.incidents::lang.form.verified'
        type: switch
    intervention:
        label: 'harassmap.incidents::lang.form.intervention'
        type: switch
    user:
        label: 'harassmap.incidents::lang.form.user'
        type: switch