columns:
    id:
        label: 'harassmap.incidents::lang.model.incident.id'
    public_id:
        label: 'harassmap.incidents::lang.model.incident.public_id'
        searchable: true
    date:
        label: 'harassmap.incidents::lang.model.incident.date'
        type: datetime
    created_at:
        label: 'harassmap.incidents::lang.model.incident.created_at'
        type: datetime
    verified:
        label: 'harassmap.incidents::lang.model.incident.verified'
        type: switch
    is_hidden:
        label: 'harassmap.incidents::lang.model.incident.is_hidden'
        type: switch
    support:
        label: 'harassmap.incidents::lang.model.incident.support'
    domain_id:
        label: 'harassmap.incidents::lang.model.incident.domain'
        select: host
        relation: domain
    description:
        label: 'harassmap.incidents::lang.model.incident.description'
        searchable: true
        invisible: true