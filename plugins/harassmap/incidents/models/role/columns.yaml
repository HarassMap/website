columns:
    domain:
        label: 'harassmap.incidents::lang.form.domain'
        relation: domain
        select: host
        width: '250px'
    name:
        label: 'harassmap.incidents::lang.form.name'
        type: text
        searchable: true