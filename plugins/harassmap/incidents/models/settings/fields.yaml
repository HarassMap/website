# ===================================
#  Field Definitions
# ===================================

tabs:
    fields:
        api_day_limit:
            label: API Limit (Per Day)
            span: left
            type: number
            tab: API