# ===================================
#  Field Definitions
# ===================================

tabs:
    fields:
        use_throttle:
            label: API Limit (Per Day)
            span: left
            type: number
            tab: API