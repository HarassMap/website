plugin:
    name: 'harassmap.incidents::lang.plugin.name'
    description: 'harassmap.incidents::lang.plugin.description'
    author: Harassmap
    icon: oc-icon-map-marker
    homepage: ''
navigation:
    harassmap.incidents:
        label: 'harassmap.incidents::lang.menu.incidents'
        url: harassmap/incidents/category
        icon: icon-map-marker
        permissions:
            - harassmap.incidents.access_categories
        sideMenu:
            harassmap.incidents.categories:
                label: 'harassmap.incidents::lang.menu.categories'
                url: harassmap/incidents/category
                icon: icon-tags
                permissions:
                    - harassmap.incidents.access_categories
            harassmap.incidents.roles:
                label: Roles
                url: harassmap/incidents/roles
                icon: icon-asterisk
    harassmap.incidents.countries:
        label: 'harassmap.incidents::lang.menu.countries'
        url: harassmap/incidents/countries
        icon: icon-globe
        sideMenu:
            harassmap.incidents.countries.country:
                label: 'harassmap.incidents::lang.menu.countries'
                url: harassmap/incidents/countries
                icon: icon-globe
            harassmap.incidents.countries.city:
                label: 'harassmap.incidents::lang.menu.cities'
                url: harassmap/incidents/cities
                icon: icon-building-o
    harassmap.incidents.domain:
        label: 'harassmap.incidents::lang.menu.domain'
        url: harassmap/incidents/domain
        icon: icon-square-o
        permissions:
            - harassmap.incidents.domain.access_content
            - harassmap.incidents.domain.access_domain
    harassmap.incidents.domain.content:
        label: 'harassmap.incidents::lang.menu.content'
        url: harassmap/incidents/content
        icon: icon-edit
        permissions:
            - harassmap.incidents.domain.access_content
    harassmap.incidents.domain.tips:
        label: 'harassmap.incidents::lang.menu.tips'
        url: harassmap/incidents/tip
        icon: icon-quote-left
permissions:
    harassmap.incidents.access_categories:
        tab: 'harassmap.incidents::lang.plugin.permissions.tab'
        label: 'harassmap.incidents::lang.plugin.permissions.access_categories'
    harassmap.incidents.domain.access_domain:
        tab: 'harassmap.incidents::lang.plugin.permissions.tab'
        label: 'harassmap.incidents::lang.plugin.permissions.manage_domains'
    harassmap.incidents.domain.access_content:
        tab: 'harassmap.incidents::lang.plugin.permissions.tab'
        label: 'harassmap.incidents::lang.plugin.permissions.manage_content_blocks'
    harassmap.incidents.domain.manage_user_domains:
        tab: 'harassmap.incidents::lang.plugin.permissions.tab'
        label: 'harassmap.incidents::lang.plugin.permissions.manage_user_domains'
