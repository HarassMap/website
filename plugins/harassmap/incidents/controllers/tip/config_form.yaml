name: Tip
form: $/harassmap/incidents/models/tip/fields.yaml
modelClass: Harassmap\Incidents\Models\Tip
defaultRedirect: harassmap/incidents/tip
create:
    redirect: 'harassmap/incidents/tip/update/:id'
    redirectClose: harassmap/incidents/tip
update:
    redirect: harassmap/incidents/tip
    redirectClose: harassmap/incidents/tip
