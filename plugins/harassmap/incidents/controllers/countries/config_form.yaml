name: Countries
form: $/harassmap/incidents/models/country/fields.yaml
modelClass: Harassmap\Incidents\Models\Country
defaultRedirect: harassmap/incidents/countries
create:
    redirect: 'harassmap/incidents/countries/update/:id'
    redirectClose: harassmap/incidents/countries
update:
    redirect: harassmap/incidents/countries
    redirectClose: harassmap/incidents/countries
