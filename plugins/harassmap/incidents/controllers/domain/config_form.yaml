name: Domain
form: $/harassmap/incidents/models/domain/fields.yaml
modelClass: Harassmap\Incidents\Models\Domain
defaultRedirect: harassmap/incidents/domain
create:
    redirect: 'harassmap/incidents/domain/update/:id'
    redirectClose: harassmap/incidents/domain
update:
    redirect: harassmap/incidents/domain
    redirectClose: harassmap/incidents/domain
