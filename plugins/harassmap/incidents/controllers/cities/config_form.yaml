name: Cities
form: $/harassmap/incidents/models/city/fields.yaml
modelClass: Harassmap\Incidents\Models\City
defaultRedirect: harassmap/incidents/cities
create:
    redirect: 'harassmap/incidents/cities/update/:id'
    redirectClose: harassmap/incidents/cities
update:
    redirect: harassmap/incidents/cities
    redirectClose: harassmap/incidents/cities
