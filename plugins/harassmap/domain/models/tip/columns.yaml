columns:
    domain:
        label: Domain
        relation: domain
        select: host
    tip:
        label: tip
        type: text
    created_at:
        label: created_at
        type: datetime
